"""Type stubs for yomail.patterns.greetings."""

def is_greeting_line(line: str) -> bool:
    """Check if a line matches Japanese greeting patterns.

    Args:
        line: A single line of text.

    Returns:
        True if the line matches a greeting pattern.
    """
    ...
