"""Type stubs for yomail.patterns.closings."""

def is_closing_line(line: str) -> bool:
    """Check if a line matches Japanese closing patterns.

    Args:
        line: A single line of text.

    Returns:
        True if the line matches a closing pattern.
    """
    ...
