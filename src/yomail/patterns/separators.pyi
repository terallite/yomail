"""Type stubs for yomail.patterns.separators."""

def is_separator_line(line: str) -> bool:
    """Check if a line is a visual separator.

    Args:
        line: A single line of text.

    Returns:
        True if the line is a visual separator.
    """
    ...
